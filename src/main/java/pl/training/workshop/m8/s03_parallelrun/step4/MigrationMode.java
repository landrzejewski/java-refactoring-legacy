package pl.training.workshop.m8.s03_parallelrun.step4;

/** Krok 4: jawny tryb migracji zamiast flagi boolean - LEGACY, SHADOW, CANDIDATE. */
public enum MigrationMode {
    LEGACY,
    SHADOW,
    CANDIDATE
}
